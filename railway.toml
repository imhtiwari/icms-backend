{
  "name": "icms-backend",
  "services": {
    "web": {
      "build": {
        "builder": "NIXPACKS"
      },
      "runtime": "java"
    }
  },
  "build": {
    "builder": "NIXPACKS"
  }
}

[env]
SPRING_PROFILES_ACTIVE = "prod"
RAILWAY_PG_PASSWORD = "your_database_password_here"
